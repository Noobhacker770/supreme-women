[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Vite is a single-page app. Keep client-side navigation working.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
